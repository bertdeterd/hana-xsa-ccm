@OData.publish : true
context CaseType {
    entity Header {
        key ID          : String(10);
            DESCRIPTION : String(100);
            ACTIVE      : Boolean;
    }
    technical configuration {
        column store;
    };
};